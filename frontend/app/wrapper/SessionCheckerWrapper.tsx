"use client";
import { useEffect } from "react";
import { useRouter } from "next/navigation";
import { usePathname } from "next/navigation";

export const SessionCheckerWrapper = ({children}:{children:React.ReactNode}) => {

  useEffect(() => {
    
  }, []);

  return <>{children}</>;
};
